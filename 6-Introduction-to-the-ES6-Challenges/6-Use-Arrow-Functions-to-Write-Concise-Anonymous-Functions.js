/* Even though I've read the module and understand not needing name functions, especially basic ones like Date(), but this is still hard to read for me */

const magic = () => {
  "use strict";
  return new Date();
};