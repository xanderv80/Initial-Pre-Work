/* After being stuck on the previous module for days, this was refreashing to do something simple again. Ada Loveless wrote the first loop in computer code */

// Setup
var myArray = [];

var i = 0;
while(i<5) {
  myArray.push(i);
  i++;
}

// Only change code below this line.

