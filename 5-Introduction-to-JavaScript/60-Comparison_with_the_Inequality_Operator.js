/* Writing commments in this section is so much easier because I have actual thoughts about what I learned, sorta like the inverse of the solving the problems. Writing comments for the HTML and CSS was hard because I really didn't have anything to say. This is like the previous module but with a 'is not' instead of 'is?' */
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);