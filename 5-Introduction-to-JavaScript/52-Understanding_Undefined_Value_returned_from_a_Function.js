/* This isn't so bad but it is a step away from assigning simple values to a variable */

// Example
var sum = 0;
function addThree() {
  sum = sum + 3;
}

// Only change code below this line

function addFive() {
   sum = sum + 5;
}


// Only change code above this line
var returnedValue = addFive();