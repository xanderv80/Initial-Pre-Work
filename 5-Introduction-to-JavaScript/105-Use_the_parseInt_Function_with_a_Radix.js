/* This module still confuses me a little, I think its because I don't understand how parseInt actually works. I've read the explination but it still seems strange to me */

function convertToInteger(str) {

  return parseInt(str, 2);
}

convertToInteger("10011");